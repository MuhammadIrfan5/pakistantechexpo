import { Division } from '../src/components/atoms';
import {VenuePage} from "../src/components/pages";

export default function Index() {
  return (
    <Division>
      <VenuePage/>
    </Division>
  )
}
